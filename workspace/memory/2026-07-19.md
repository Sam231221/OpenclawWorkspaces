- Tightened the production `youtube-linkedin-review-publish` workflow after Mr. Sam reported Telegram packet formatting issues. Updated `AGENTS.md`, `workspace-linkedinengine/AGENTS.md`, `docs/youtube_linkedin_workflow.md`, live cron job `fe974333-bc3a-484a-aba3-679aa3102702`, and canonical state version 36.
- New packet delivery rules: attach each generated thumbnail/primary image as rendered Telegram media when a media-capable tool is available; do not rely on YouTube link previews; disable webpage/link previews for YouTube URLs where supported; avoid a bare clickable YouTube URL at the end of packet messages; record any text-only fallback in run metadata.
- New no-transcript source rule: do not generate approval-ready packets from the YouTube title plus generic search snippets alone. If no captions/transcript and no reliable supporting source pages are available, mark `insufficient_source_context` and skip or hold for manual review.
- Audited newest packets: `Noo0NWD0gHU` and `Ee8APcJ-jwc` had limited but supporting context beyond title/snippets; `zZ5-KVDIaPg` was based on title plus search snippets only, so it was marked `needs_revision` with `source_confidence=insufficient`.
- Noted the live cron currently has `lastRunStatus=error` and `consecutiveErrors=2` from the isolated agent stopping after a failed broad search. Did not force a cron rerun because that could send Telegram output.
- Tightened the workflow again after Mr. Sam clarified that LinkedIn content must be generated from YouTube captions/transcripts, not title/search snippets or supporting source pages alone. Added local helper `/home/sam/.openclaw/workspace/scripts/fetch_youtube_transcript.mjs` plus npm packages `youtube-caption-extractor` and `youtube-captions-scraper`; cron now requires `npm run youtube:transcript -- <youtube-url-or-id>` fallback before marking a candidate insufficient.
- Updated live cron `fe974333-bc3a-484a-aba3-679aa3102702`, `AGENTS.md`, `workspace-linkedinengine/AGENTS.md`, `docs/youtube_linkedin_workflow.md`, and state metadata so approval-ready packets require usable transcript/caption text. Supporting pages/reporting may enrich or fact-check only after transcript text exists.
- Applied the stricter audit to newest no-transcript packets: `pkt_zZ5-KVDIaPg_v1`, `pkt_Noo0NWD0gHU_v1`, and `pkt_Ee8APcJ-jwc_v1` are `needs_revision`/insufficient until transcript retrieval succeeds and packet content is regenerated from transcript. `zZ5-KVDIaPg` had one transient successful transcript fetch during testing, but retry-enabled final checks failed with YouTube `LOGIN_REQUIRED`/caption-not-found responses, so do not treat it as reliably available without re-fetching.
