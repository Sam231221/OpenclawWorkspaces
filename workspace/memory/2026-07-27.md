# 2026-07-27

- Created a pending Skill Workshop proposal named `capture-to-calendar-and-reminders-20260727-70249d1a18` for Mr. Sam's "Capture to Calendar and Reminders" workflow.
- Verified OpenClaw image understanding with a harmless synthetic event-text image; OCR succeeded.
- Found Google Calendar reference under the Maton API gateway skill and confirmed `MATON_API_KEY` is present, but no active `google-calendar` connection exists.
- Requested installation/connection for the `google-calendar@openai-curated` plugin; user confirmation was not completed in-tool.
- Found the official Apple Reminders skill at `/home/sam/.npm-global/lib/node_modules/openclaw/skills/apple-reminders/SKILL.md`, but the current host is Debian Linux and `remindctl` is not installed; the skill is macOS-only.
- Next step: Mr. Sam needs to connect Google Calendar and provide/enable a macOS Reminders execution path with `remindctl authorize` before harmless write tests can run.
- 17:20 UTC update: Mr. Sam connected Google Calendar via Maton. Read verification succeeded, and the writable primary calendar is `sameershahithakuri123@gmail.com`. Revised the pending Skill Workshop proposal with this status. Calendar still needs explicit approval for a harmless write test.
- 17:27 UTC update: Mr. Sam asked to remove Apple Reminders from the scheduling workflow. Revised the pending proposal so it is strictly Google Calendar-only; Apple Reminders and reminder/task creation are no longer part of it.
- 17:34 UTC update: Mr. Sam approved the harmless Google Calendar write test. Duplicate search found no `OpenClaw Calendar Test` on 2026-07-28, then created event ID `q2pcn3jd4urhkt6grokt0kikj4` for 10:00-10:15 Europe/London. Added audit log entry to `state/capture_google_calendar_audit.jsonl` and revised the pending proposal with the passed write test.
- 17:49 UTC update: First real Capture to Google Calendar save succeeded from a screenshot: UWE careers event `CVs, application forms and recruitment for summer internships and graduate schemes`, 2026-08-10 12:00-13:00 Europe/London, location Online, event ID `a6gch47ds2da0kr8uc5mthuhlc`. Audit log updated. Successful classified saves after setup test: 1/5 before asking about auto-save.
- 17:42 UTC update: Mr. Sam confirmed he wants the Gym Notion page reorganized as a fitness/nutrition knowledge hub, not a workout tracker. Maton Notion connection is active, but searches for `Gym`, `Nutrition`, `Food`, `Protein`, `Fitness`, `Calories`, `vitamins`, `nutrients`, `diet`, `carbs`, `minerals`, and `supplements` did not locate the target page. At 17:48 he provided `https://app.notion.com/p/Gym-39e2fa87f29d80069afeca978c0525a6`; direct `page get` and `block children` still returned Notion 404 "Make sure the relevant pages and databases are shared with your integration Maton." Need the Gym page shared with the Maton Notion connection before editing.
- 18:16 UTC update: Mr. Sam shared the Gym Notion page with Maton. Reorganized `https://app.notion.com/p/Gym-39e2fa87f29d80069afeca978c0525a6` in place as a fitness/nutrition knowledge hub: added 181 clean blocks, archived 344 old scattered top-level blocks with zero failures, and verified the final heading outline. Main sections now cover core principles, nutrients, macros, vitamins/minerals, calories/body fat, food labels, pre/post-workout meals, food maps, supplements, and original sources.
- 20:24 UTC update: Ran `/home/sam/.npm-global/bin/openclaw doctor --fix --yes` for server health. Doctor refreshed plugin registry and disabled unusable `book-to-notion`; config updated at `~/.openclaw/openclaw.json` with backup `~/.openclaw/openclaw.json.bak`. Gateway healthy on loopback `127.0.0.1:18789`; Telegram OK; OpenClaw update `2026.7.1-2` available; remaining warnings include plaintext config secrets, `gateway.controlUi.allowInsecureAuth=true`, unpinned `@openclaw/codex`, and cron job `youtube-linkedin-review-publish` last error `stream disconnected before completion`.
## OpenClaw SecretRef hardening - 20:50 UTC

- Migrated plaintext `gateway.auth.token` and `channels.telegram.botToken` out of `~/.openclaw/openclaw.json` into file-backed SecretRefs via provider `secrets.providers.localfile`.
- Created `~/.openclaw/secrets.json` with mode `600`; did not record secret values in memory.
- Disabled `gateway.controlUi.allowInsecureAuth`; current config validation passes.
- Restarted `openclaw-gateway.service`; new PID observed as `1503811`, started at `2026-07-27 20:35:36 UTC`.
- `openclaw secrets audit` now reports `plaintext=0`, `unresolved=0`, `shadowed=0`, with only OAuth legacy residue out of scope for static SecretRef migration.
- `youtube-linkedin-review-publish` cron has failed four consecutive scheduled runs after ~402-405s with `stream disconnected before completion` from `https://chatgpt.com/backend-api/codex/responses`; earlier successful runs reached transcript-gate handling.
- Added one-shot follow-up cron `check-youtube-linkedin-next-run` (`60eb2197-9bc6-43ef-a3b0-d56f049bac32`) for `2026-07-28T00:15:00Z` to inspect the next scheduled run and investigate only if it fails again.
- Remaining wrinkle: local CLI device `dd619...a1a9` is paired with limited write scope, causing admin/deep probes to request a scope upgrade. Runtime health is OK; avoid manual device registry edits unless explicitly needed.

## OpenClaw Memory Setup Audit - 20:51 UTC

- Reviewed official OpenClaw memory docs and local server state for a best-practice memory configuration recommendation.
- Current server is OpenClaw `2026.6.1`; official docs are current/latest and use the newer top-level `memory.search` terminology, while this install also exposes local-version-compatible `agents.defaults.memorySearch` keys.
- Current setup uses builtin `memory-core` with OpenAI `text-embedding-3-small`, hybrid search, sqlite-vec available, and one SQLite index per agent under `~/.openclaw/memory/`.
- `memory-core` is enabled; `active-memory` and `memory-wiki` are installed but disabled.
- Main memory files are modest, but `openclaw memory status --deep --json` showed dirty or missing index metadata for `main` and `notionknowledgeos`; recommended first maintenance action is `openclaw memory index --force` for all agents.
- Recommended conservative direction: keep builtin memory, enable MMR and temporal decay, enable compaction memory flush explicitly, consider active-memory only for `main` direct chats, keep transcript recall disabled broadly because `session.dmScope` is `per-channel-peer`, and do not enable memory-wiki unless Mr. Sam wants a local wiki layer in addition to Notion Knowledge OS.
