# Memory: 2026-06-07

## OpenClaw Multi-Agent Cost Setup

- Mr. Sam asked to implement the multi-agent cost-optimization plan using OpenAI
  models, not Anthropic.
- Updated `AGENTS.md` so Saara remains the main dispatcher and safety controller.
- Added cost routing rules:
  - Saara/default dispatch: `openai/gpt-5.4-mini`, reasoning low.
  - Simple classification/extraction: `openai/gpt-5.4-nano`, reasoning
    none or low.
  - Specialist agents: `openai/gpt-5.4-mini`, reasoning low/medium.
  - Complex work: escalate to `openai/gpt-5.4`.
  - Critical or repeatedly failed work: escalate to `openai/gpt-5.5`.
- Added a fallback rule: if a low-cost OpenAI model is not registered, use the
  nearest cheaper available OpenAI model and mention the fallback.
- Added heartbeat/runtime guidance: heartbeat and status polls should not call an
  LLM; Saara handles runtime, cron, server config, model registration, and local
  workspace instruction edits directly.
- Updated `HEARTBEAT.md` to prefer local/no-LLM heartbeat handling and use the
  cheapest fast OpenAI model only if an LLM is invoked anyway.
- Updated `/home/sam/.openclaw/openclaw.json` so Saara and both specialists
  default to `openai/gpt-5.4-mini` with `openai/gpt-5.4` and `openai/gpt-5.5`
  as fallbacks, and registered `openai/gpt-5.4-nano`, `openai/gpt-5.4-mini`,
  `openai/gpt-5.4`, and `openai/gpt-5.5` in the default model map.
- Added context/memory rules: load only `SOUL.md`, `USER.md`, `IDENTITY.md`, and
  today dated memory on session start; search prior memory on demand; store only
  distilled notes, decisions, links, blockers, and next steps.
- Added a minimal specialist delegation envelope and operational guardrails for
  subagent depth, output size, browsing, escalation, and failed tool loops.

## Follow-Up

- Earlier memory showed a previously configured provider model failed because the
  provider model was not registered. If Saara still fails on startup, check the
  OpenClaw model registry/provider config and register the low-cost model IDs or
  point defaults to the nearest available OpenAI model.

## Specialist Agent Memory Setup

- Checked current OpenClaw docs on multi-agent routing, agent workspaces, config,
  and memory. Key points: each agent should keep an isolated workspace,
  `agentDir`, auth/session store, and workspace-local memory; `MEMORY.md` is the
  curated durable layer, while `memory/YYYY-MM-DD.md` is the daily working log.
- Updated `/home/sam/.openclaw/openclaw.json` defaults for lower cost:
  heartbeat uses `openai/gpt-5.4-nano`, no reasoning, light context; image
  quality is `efficient`; thinking defaults to `low`; reasoning defaults to
  `off`; memory/tool result context limits are bounded.
- Rewrote `workspace-linkedinengine/AGENTS.md` with LinkedIn-specific routing,
  OpenAI-only cost escalation, memory rules, and a no-publishing-without-current
  approval boundary.
- Rewrote `workspace-notionknowledgeos/AGENTS.md` with Knowledge OS/Notion scope,
  OpenAI-only cost escalation, memory rules, and distilled-note handling for
  PDFs/screenshots/research.
- Added specialist `MEMORY.md` and `memory/2026-06-07.md` files for both
  `linkedinengine` and `notionknowledgeos`.
- Simplified `workspace-notionknowledgeos/HEARTBEAT.md` so routine specialist
  heartbeat work is skipped unless Mr. Sam explicitly adds checks.

## Saara Long-Term Memory Created

- Mr. Sam noticed Saara's root `MEMORY.md` was missing from the main workspace.
- Created `/home/sam/.openclaw/workspace/MEMORY.md` as Saara's compact curated
  long-term memory, separate from the daily working log.
