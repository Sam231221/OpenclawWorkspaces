- Fixed the YouTube Daily Watchlist Telegram preview issue after Mr. Sam reported
  that a Theo `https://t3.gg/` card was being attached to the cron digest.
  Root cause: the digest rendered `Theo / t3.gg` as plain text while Telegram
  link previews were not explicitly disabled. Updated
  `scripts/send_youtube_digest.mjs` to send Telegram HTML with
  `disable_web_page_preview: true` and `link_preview_options.is_disabled: true`.
  Updated `scripts/youtube_linkedin_daily_digest.mjs` so digest-visible Theo
  channel labels render as `Theo / t3dotgg`, while preserving the canonical
  channel metadata. Updated `docs/youtube_linkedin_workflow.md`. Verified with
  `npm --silent run youtube:digest -- --dry-run --now 2026-08-15T00:02:00Z`,
  `npm --silent run youtube:digest:send:dry-run -- --skip-send --now
  2026-08-15T00:02:00Z`, and `npm --silent run youtube:digest:health`.
- Tightened the YouTube Daily Watchlist formatting so all digest/save/status
  paths use a shared `Watch video` HTML link helper instead of repeating raw URL
  formatting. Updated the workflow doc to state that each video should have one
  linked `Watch video` line and no separate raw YouTube URL/source-preview
  lines. Verified again with digest dry-run, send dry-run, and health check.
- Sent Mr. Sam a Telegram review copy of the current YouTube Daily Watchlist
  digest with HTML parsing and link previews disabled; Telegram returned
  message ID `221`.
- Updated the YouTube Daily Watchlist digest format: removed the `Scan details`
  block, added Europe/London time to the header, added a `Daily send: 06:00
  Europe/London` line, changed summary items to bold counts with italic labels,
  and changed channel breakdown to lettered rows (`A.`, `B.`, `C.`). Telegram
  does not support true font family or font-size controls, so the implemented
  hierarchy uses Telegram-safe HTML (`b`, `i`, links, and code). Changed the
  systemd user timer from `00:00 UTC` with a 5-minute randomized delay to exact
  `06:00 Europe/London`, reloaded and restarted it. Because `Persistent=true`,
  the first restart triggered one immediate catch-up real send; Telegram
  returned message ID `222`. Verified the final next timer as 2026-08-16
  05:00:00 UTC, equivalent to 06:00:00 Europe/London.
