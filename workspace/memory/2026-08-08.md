## Telegram Calendar Capture

- Confirmed the `capture-to-calendar-and-reminders` skill is live in the
  workspace and referenced by `AGENTS.md`.
- Hardened `AGENTS.md` so Telegram direct chat calendar capture explicitly
  includes the Saara bot chat.
- Verified Maton Google Calendar OAuth connection is ACTIVE via
  `/home/sam/.npm-global/bin/maton`.
- Verified Google Calendar listing works and the primary calendar is writable.

## Telegram Vocabulary Screenshot Capture

- Mr. Sam clarified the intended skill was
  `save-vocabulary-screenshots-to-notion`.
- Added explicit dispatcher coverage for vocabulary screenshots/images in
  Telegram Saara bot chat.
- Added a Telegram Saara bot chat note to the skill and recorded the reachable
  Vocabulary root page ID `fd52fa87-f29d-8303-83fa-8169ea14008c`.
- Verified Maton Notion OAuth is ACTIVE and `maton notion whoami` reaches
  `Sameer Shahi's Notion`.
- Mr. Sam later reported the skill created word pages and a `Verbs` page, and
  returned saved info to OpenClaw chat instead of Telegram. Patched the skill
  and dispatcher so vocabulary entries must be headings/blocks inside semantic
  category pages, grammar belongs in `Type`, no word/part-of-speech pages are
  created, and Telegram-triggered runs must report back to the Telegram source
  chat.
- Read-only Notion check found accidental structure:
  `Vocabulary` -> `Verbs` (`3b62fa87-f29d-8100-9c90-ea81c45c5839`) -> `edify`
  (`3b62fa87-f29d-81e3-9318-ca10b5179486`). Do not move/delete without Mr.
  Sam's explicit approval.
- Mr. Sam approved fixing that bad Notion structure. Created semantic category
  `Education` (`3b62fa87-f29d-8179-9ab3-dab6c610a66e`) under `Vocabulary`,
  appended the preserved `edify` entry as 20 blocks inside it, verified the
  entry, and archived the accidental `Verbs` page. The separate `edify` page
  archive call failed only because its `Verbs` ancestor was already archived.
