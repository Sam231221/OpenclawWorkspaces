## YouTube to LinkedIn workflow

- Cron run `run_youtube_linkedin_20260722_103500Z` checked Theo/t3.gg, ThePrimeTime, and Fireship.
- Selected three relevant long-form candidates for transcript retrieval: `xmGY276gEFY`, `0F5zG1RQ4Ig`, and `M51asSwRLxA`.
- No approval-ready LinkedIn packets were created because all selected candidates failed the transcript-required source gate after the local fallback chain.
- Caption failures were YouTube `LOGIN_REQUIRED` / missing captions; audio transcription also failed because `linkedinengine` lacks the transcribe skill and the retry via main `CODEX_HOME` hit YouTube bot/auth gating.
- Canonical state updated to version 39 with the run record, seen videos, and `insufficient_source_context` metadata.
## YouTube to LinkedIn workflow

- Cron run `run_youtube_linkedin_20260722_103500Z` checked Theo/t3.gg, ThePrimeTime, and Fireship.
- Selected three relevant long-form candidates for transcript retrieval: `xmGY276gEFY`, `0F5zG1RQ4Ig`, and `M51asSwRLxA`.
- No approval-ready LinkedIn packets were created because all selected candidates failed the transcript-required source gate after the local fallback chain.
- Caption failures were YouTube `LOGIN_REQUIRED` / missing captions; audio transcription also failed because `linkedinengine` lacks the transcribe skill and the retry via main `CODEX_HOME` hit YouTube bot/auth gating.
- Canonical state updated to version 39 with the run record, seen videos, and `insufficient_source_context` metadata.

## 2026-07-22 Workflow Updates

- `scripts/fetch_youtube_transcript.mjs` now uses a full fallback chain: YouTube captions, `youtube-captions-scraper`, yt-dlp audio download, then the transcribe skill.
- The transcribe skill CLI now has a direct OpenAI HTTP fallback for transcription when the Python `openai` package is unavailable.
- yt-dlp support now accepts cookies via `YOUTUBE_DL_COOKIES_FILE` and `YOUTUBE_DL_COOKIES_FROM_BROWSER` for blocked YouTube videos.
- The packet/state store was reset to a clean slate for a fresh from-zero test run: packets, variants, visuals, carousels, runs, seen items, and related YouTube asset references were cleared from workspace state.
- The working assumption going forward is that cookie-authenticated yt-dlp access is the remaining prerequisite for fully end-to-end testing of blocked YouTube videos.
- The best-practice procedure is now explicit in the workflow docs and helper usage banner: caption helper first, then cookie-authenticated retry if needed, then automatic audio transcription fallback, and finally `insufficient_source_context` when no usable transcript can be recovered.
- The helper was refactored to use `yt-dlp` subtitle download before the older caption extractors; testing `FjLn_yh55dU` still failed at the YouTube bot/auth gate on subtitle, caption, and audio retrieval without cookies.
- Installed the ClawHub `youtube-transcript-generator` skill in both the main and `linkedinengine` workspaces. Refactored `scripts/fetch_youtube_transcript.mjs` into a JSON compatibility wrapper around that skill and removed the old `youtube-caption-extractor` / `youtube-captions-scraper` dependencies from the active workflow. The live cron prompt, dispatcher rules, specialist rules, docs, and state metadata now point at the skill-backed transcript path.

## YouTube Captions Skill Proposal

- Created pending Skill Workshop proposal `youtube-captions-20260722-f420f19066` for a workspace skill that uses `yt-dlp` public captions first, YouTube auto captions second, and optional local Whisper fallback without browser automation, cookies, OAuth, API keys, or YouTube account access.
- Proposal includes `SKILL.md` plus `scripts/youtube_captions.py`; helper forces `yt-dlp --ignore-config`, rejects playlist-only URLs, labels sources as creator captions, automatic YouTube captions, or local Whisper, and returns JSON output.
- Local environment check showed `yt-dlp`, `ffmpeg`, and `whisper` are not currently installed, so runtime testing remains pending until dependencies are installed and the proposal is applied.
- Mr. Sam approved the skill. Applied it live as workspace skill `youtube-captions-generator`, corrected the installed frontmatter and directory name, installed user-local `yt-dlp`, `openai-whisper`, and an `imageio-ffmpeg`-backed `ffmpeg` wrapper under `~/.local/bin`.
- Verified `openclaw skills list` marks `youtube-captions-generator` ready when run with the gateway service PATH. Smoke-tested caption extraction on `dQw4w9WgXcQ`, producing creator-caption TXT and SRT files under `captions-test*`.
- Tested user URL `imrv3ze3jeY` with `youtube-captions-generator`; anonymous `yt-dlp` access was blocked, so no captions or Whisper fallback transcript could be produced under the no-cookies/no-login rules.
- Installed user-local Python package `youtube-transcript-api==1.2.4` and tested video `imrv3ze3jeY`; it failed with `RequestBlocked`, reporting that YouTube is blocking requests from the server IP before transcripts can be listed.
- Installed ClawHub skill `youtube-transcript@1.0.1` at `skills/youtube-transcript`. Testing `imrv3ze3jeY` failed because the skill requires WireGuard tooling/config (`wg-quick`, `wg0`, source IP `10.100.0.2`) and this server currently has no `wg0` interface and no `wg-quick` binary.

## Notion Knowledge Notes

- Created Notion page `WireGuard and Tailscale Residential Exit for YouTube Transcript Automation` under parent `SSH tunnel`: https://app.notion.com/p/WireGuard-and-Tailscale-Residential-Exit-for-YouTube-Transcript-Automation-3a52fa87f29d81519119ed46bbb533ce
- Page documents purpose, theory category, plain-English summary, WireGuard step-by-step configuration, Tailscale exit-node alternative, workflows/flowcharts, troubleshooting checklists, observed setup status, and next actions for YouTube transcript automation via residential egress.
- Mr. Sam asked to remove server WireGuard-related config/files. Removed the user-owned ClawHub `youtube-transcript` skill folder and its `.clawhub/lock.json` entry. Could not remove root-owned `/etc/wireguard`, disable `wg-quick@wg0`, or bring down `wg0` because sudo requires a password in this session; remaining active state observed: `wg0` with `10.100.0.2/24`, policy rule from `10.100.0.2` to table `51820`, and default route in table `51820` via `wg0`.
