- Investigated `youtube-linkedin-review-publish` after Mr. Sam reported the daily Telegram digest stopped after Tuesday. Live cron `fe974333-bc3a-484a-aba3-679aa3102702` is enabled, scheduled daily at `0 0 * * *` UTC with `staggerMs=300000`, targets `linkedinengine`, and delivers to Telegram `8692887396`.
- Root cause found in cron/session logs: Aug 5 through Aug 10 scheduled runs all fail before any assistant text or tool call. Each failed session has only repeated user prompt entries, zero tool events, zero assistant output, and trajectory `promptError="stream disconnected before completion: error sending request for url (https://chatgpt.com/backend-api/codex/responses)"`. Runtime tries `gpt-5.4-mini`, then `gpt-5.4`, then `gpt-5.5`; each candidate fails at `stage=prompt`.
- The digest helper itself is healthy: `npm --silent run youtube:digest -- --dry-run --now 2026-08-10T00:01:00Z` succeeded locally and produced a normal digest. `--dry-run` does not write state. The failure is in the OpenClaw isolated-agent/model-stream layer before the exact command can run.
- Related observations: gateway logs also show repeated Telegram `UND_ERR_CONNECT_TIMEOUT`/health-monitor restarts around the cron windows, but shell probes to `https://chatgpt.com/backend-api/codex/responses` and `https://api.telegram.org` worked at investigation time. `openclaw doctor --lint --json` returned only unrelated warnings plus `AGENTS.md` near bootstrap limit.
- Implemented the best-practice fix for the failed `youtube-linkedin-review-publish` digest. Added `scripts/send_youtube_digest.mjs` to run the existing RSS digest, send it to Telegram through the OpenClaw CLI, chunk long messages, retry sends, send a failure alert when possible, and write JSONL audit records to `state/youtube_digest_runs.jsonl`. Added `scripts/youtube_digest_health.mjs` and npm scripts `youtube:digest:send`, `youtube:digest:send:dry-run`, and `youtube:digest:health`.
- Installed and enabled the user systemd timer `openclaw-youtube-linkedin-digest.timer` with source units in `systemd/user/`; it runs daily at 00:00 UTC with up to 5 minutes randomized delay and `Persistent=true`. Disabled the legacy OpenClaw isolated-agent cron `fe974333-bc3a-484a-aba3-679aa3102702` so the deterministic digest no longer depends on Codex prompt streaming. Health check passed; next timer fire showed `2026-08-11T00:01:41Z`.
