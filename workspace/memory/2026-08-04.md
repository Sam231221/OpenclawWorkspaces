
## 2026-08-04 08:54 UTC - Pre-Compaction Flush

- Investigated broken `youtube-linkedin-preview-publish` / `youtube-linkedin-review-publish` workflow.
- Root failure observed: 14 consecutive cron runs died before the specialist made its first tool call due to Codex response stream disconnects.
- Changed the live `youtube-linkedin-review-publish` cron into stability mode: `openai/gpt-5.4-mini`, low reasoning, bounded to one packet, active-state-only, no archive reads, and 10-minute timeout.
- Validation succeeded: manual cron run completed `ok`, consecutive errors reset to `0`, packet `pkt_434cG4g5KLE_v1` created, variant `434cG4g5KLE_builder_insight_v1` created, PNG visual `pkt_434cG4g5KLE_v1_primary_png` created, and Telegram review packet delivered as message `184`.
- Files updated during the workflow repair: `docs/youtube_linkedin_workflow.md`, `state/youtube_linkedin_workflow.json`, and `assets/youtube-linkedin/thumbnails/pkt_434cG4g5KLE_v1_primary.png`.
- Outcome reported to Mr. Sam: workflow fixed and validated.

## 2026-08-04 10:05 UTC - YouTube Digest-Only Redesign

- Mr. Sam identified product-level issues in the YouTube-to-LinkedIn cron:
  unclear seen logic, possible one-channel bias, automatic packet/media buildup,
  legacy phase naming confusion, and hard-to-read Telegram messages.
- Changed the live `youtube-linkedin-review-publish` cron to daily digest-only:
  it now runs `cd /home/sam/.openclaw/workspace && npm --silent run youtube:digest`
  as isolated `linkedinengine` on `openai/gpt-5.4-mini` with low reasoning.
- Added deterministic helper `scripts/youtube_linkedin_daily_digest.mjs` and
  `npm --silent run youtube:digest`. It checks hard-pinned RSS feeds for Theo/t3.gg,
  ThePrimeTime, and Fireship, records compact discovery state, and does not
  fetch transcripts, create packets, create drafts, store YouTube thumbnails,
  generate visuals, or prepare publishing.
- Added direct watchlist commands: `--save-video`, `--unsave-video`, and
  `--status-video`. Saara should handle save/status locally; manual LinkedIn
  drafts still route to `linkedinengine` and require TranscriptAPI.
- Updated `AGENTS.md`, `workspace-linkedinengine/AGENTS.md`,
  `docs/youtube_linkedin_workflow.md`, and
  `docs/youtube_linkedin_workflow_deep_dive.md` to reflect the digest-first,
  manual-draft-only workflow.
- Validation passed with `node --check`, state JSON parse, digest dry-run over
  all configured channels, and read-only status command check.
- Follow-up formatting change: removed the explanatory "no drafts/packets"
  lines from the Telegram digest, added total/per-channel counts, validated the
  digest helper, and sent a Telegram test message as message `185`.
