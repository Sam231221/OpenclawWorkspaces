- Fixed the YouTube-to-LinkedIn daily digest scheduler after Mr. Sam asked for a code review and robustness pass on the retired `youtube-linkedin-review-publish` cron workflow.
- Root cause found in recent systemd journal/audit records: the deterministic digest ran, but Telegram delivery failed on Aug 11, Aug 12, and Aug 13, 2026 with OpenClaw gateway scope/pairing approval errors and timeouts.
- Updated `scripts/send_youtube_digest.mjs` so gateway scope/pairing/timeout warnings are treated as delivery failures even if a dry-run CLI exits 0, and send results now include `ok`.
- Updated `scripts/youtube_digest_health.mjs` so health checks include Telegram send dry-run delivery, systemd service/timer status, legacy cron disabled state, latest audit record, and latest real non-dry-run send. This prevents dry runs from hiding a failed scheduled run.
- Updated `systemd/user/openclaw-youtube-linkedin-digest.service` and installed it to `~/.config/systemd/user/` with explicit `Wants/After=openclaw-gateway.service`; reloaded user systemd and reset failed service state.
- Ran a live sender pass at 2026-08-13T10:52 UTC. Telegram accepted it with message ID 194, state committed after delivery, and `npm --silent run youtube:digest:health` passed all checks.
- Updated the YouTube daily watchlist Telegram format in `scripts/youtube_linkedin_daily_digest.mjs` to emit Telegram-compatible HTML with escaped dynamic content, human-readable dates, channel-grouped numbered videos, clickable `Watch video` links, compact commands, and the wording `Recent videos you may have missed`.
- Updated `scripts/send_youtube_digest.mjs` so scheduled Telegram sends pass `--delivery '{"parseMode":"HTML"}'` and split messages up to Telegram's 4,096-character limit with `Part X of Y` labels. Sent a non-mutating dry-run preview manually to Telegram at 2026-08-13T11:46 UTC; Telegram accepted it as message ID 195.
