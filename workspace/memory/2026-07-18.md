- Updated the `youtube-linkedin-review-publish` workflow wording so packet creation now explicitly requires the final delivered Telegram message to include the full review packet and exact next commands like `Approve {variant_id}`.
- Touched the canonical workflow doc and live state metadata to keep the cron prompt and recorded rules aligned.
- Tightened Saara and `notionknowledgeos` AGENTS.md rules for transcript-to-Notion-note requests: Saara now routes explicit or implied transcript/book-style Notion note tasks to `notionknowledgeos` with `openai/gpt-5.5` medium reasoning, and the Notion specialist now requires polished book-style pages with headings, callouts, toggles, useful tables, takeaways, action items, tags, placement, source nuance, and coverage-limit notes.
- Updated the production YouTube-to-LinkedIn cron job `youtube-linkedin-review-publish` (`fe974333-bc3a-484a-aba3-679aa3102702`) so the isolated `linkedinengine` run defaults to `openai/gpt-5.5` with medium reasoning for higher-quality post generation, revisions, visual planning, summaries, and publish preflight. Kept nano allowed only for simple classification/scoring/tagging/extraction/status. Synced `AGENTS.md`, `workspace-linkedinengine/AGENTS.md`, `docs/youtube_linkedin_workflow.md`, and canonical state metadata.
- Cleaned stale/unavailable model ID mentions from active OpenClaw memory and specialist docs after Mr. Sam confirmed the ChatGPT Plus/OAuth UI only exposes the smaller live model set. Updated memory notes, `workspace-linkedinengine/AGENTS.md`, `workspace-notionknowledgeos/AGENTS.md`, `workspace-notionknowledgeos/SOUL.md`, `/home/sam/.openclaw/memory/2026-05-01.md`, and the three agent-local `openai-docs/references/latest-model.md` helper copies. Verified no `gpt-5.2`, `gpt-5.3`, `gpt-5.5-pro`, or `openai-codex/...` references remain outside ignored cache/session/log/plugin-temp areas.
- Cleaned low-risk OpenClaw server files after Mr. Sam approved removal if server-safe: deleted 371 old main-agent session `*.bak-*` files (~1.42GB) and three agent `codex-home/.tmp` plugin checkout folders (~264MB total). Left credentials, state, memory, SQLite logs, `node_modules`, and generated media untouched. `/home/sam/.openclaw` dropped to ~921MB and root filesystem usage dropped from 15% to 13%.
