# 2026-08-18

- Reviewed `/home/sam/.openclaw/workspace/skills/save-vocabulary-screenshots-to-notion` after Mr. Sam reported Notion entries keeping literal markdown markers such as `**Type:**`. Diagnosis: the skill tells agents to build markdown-like vocabulary templates, but Maton Notion writes require explicit block JSON and `rich_text.annotations`; plain markdown in `rich_text.content` is preserved literally. Recommended fix: add a Notion block-writing contract forbidding raw markdown markers for formatting and requiring annotated rich_text, heading block types, and list block types.
- Applied the vocabulary skill fix directly because Skill Workshop could not resolve the older workspace skill by name/path. Updated `SKILL.md`, converted the risky Markdown-looking required-structure templates in `references/vocabulary-entry-rules.md` into semantic field-order templates, added `references/notion-block-rendering.md` with the Notion block JSON contract/example, and validated with `quick_validate.py`.
- Used `notion-maton-gateway` plus `save-vocabulary-screenshots-to-notion` to rewrite the Notion Vocabulary page `Describing Quality` (`3b82fa87-f29d-81c6-8a67-dcbf86d269f6`). Updated 80 existing blocks and inserted 8 missing `Usage Notes`/`My Note` blocks across four existing sections: duplicate `inane`, duplicate `ethereal`. Verified zero literal markdown marker hits and all field-label spans bolded via Notion annotations. Duplicates remain intentionally because deleting/merging them needs explicit approval.
