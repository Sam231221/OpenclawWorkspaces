# 2026-06-07

- Configured `notionknowledgeos` as a cost-sensitive Notion and Knowledge OS
  specialist.
- Workspace memory follows OpenClaw best practice: daily working notes in
  `memory/YYYY-MM-DD.md`, compact durable notes in `MEMORY.md`.
- The agent should use OpenAI models only, default to `openai/gpt-5.4-mini`,
  use `openai/gpt-5.4-nano` for simple extraction/classification/routing, and
  escalate only when justified.
- For external references, screenshots, PDFs, and research, store distilled
  notes, decisions, links, blockers, and next steps rather than raw documents.
- Notion access: `/home/sam/.openclaw/.env` contains `NOTION_API_TOKEN`.
  Read-only API smoke tests passed against `/v1/users/me` and `/v1/search`;
  the bot is `TestoOpenclaw` in Sameer Shahi's Notion workspace. Do not store
  or print the token.
